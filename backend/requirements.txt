# LLM & Agent framework
pydantic-ai[google,groq]>=0.0.54
pydantic>=2.7

# HTTP & async
httpx>=0.27
aiohttp>=3.9
aiofiles>=24.1
aiosqlite>=0.20

# HTTP API server (FastAPI layer over run_orchestrator)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Rate limiting
aiolimiter>=1.1

# Environment variables
python-dotenv>=1.0

# Memory — Vector (pinecone-client was renamed to pinecone in v3+)
pinecone>=5.0

# Memory — Embeddings
google-generativeai>=0.8

# Hashing
imagehash>=4.3
Pillow>=10.0

# Web tooling (from lib dependencies)
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2

# Re-ranking (removed sentence-transformers/torch — unused, 3GB+ bloat)

# WHOIS
python-whois>=0.9

# CLI
loguru>=0.7
typer>=0.12
rich>=13.7

# Testing
pytest>=8.2
pytest-asyncio>=0.23

# Media tools (external binaries — not pip)
# yt-dlp: install separately → `pip install yt-dlp`
# ffmpeg: install separately → system package manager
